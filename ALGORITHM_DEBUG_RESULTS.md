# 🔍 算法调试结果和修复方案

## 📅 调试日期: 2025-10-28

---

## 🎯 调试命令

要运行调试脚本，请在 `main.gd` 的 `_ready()` 方法中启用以下代码：

```gdscript
# 取消注释这些行
print("【启用算法调试模式】")
DebugWinChecker.run_all_debug()
get_tree().quit()
```

**已启用** ✅

---

## 📊 预期输出分析

### 测试1: 清一色胡牌

**输入手牌**: 万1万1万1 万2万3万4 万5万6万7 万8万9万9万9 (14张)

**预期结果**:
```
✓ 能胡牌（清一色）
眼睛: 万1
面子:
  - 刻子 (万1万1万1)
  - 顺子 (万2万3万4)
  - 顺子 (万5万6万7)
  - 顺子 (万8万9万...等等)
```

**实际结果** (根据测试输出):
- [ ] 通过 ✅
- [ ] 失败 ❌ (需要修复)

---

### 测试2: 清一色变体

**输入手牌**: 万2万2万2 万3万4万5 万6万7万8 万9万9 万1万1万1 (14张)

**预期结果**:
```
✓ 能胡牌
眼睛: 万1或万9或万2等
面子组合多种...
```

**实际结果** (根据测试输出):
- [ ] 通过 ✅
- [ ] 失败 ❌ (需要修复)

---

### 测试3: 听牌检测

**输入手牌**: 万1万1 万2万3万4 筒5筒5筒5 条6条7条8 条9 (13张)

**预期结果**:
```
✓ 能听牌
听牌数量: N
听牌列表:
  - [能听的各种牌]
```

**实际结果** (根据测试输出):
- [ ] 通过 ✅
- [ ] 失败 ❌ (需要修复)

---

## 🛠️ 根本原因分析

### 问题假设

**如果清一色检测失败**:

原因可能是:
1. **对子选择冲突** - 眼睛选了万1，但面子也需要万1
2. **递归逻辑bug** - `_can_form_melds()` 没有正确处理某个花色
3. **顺子边界问题** - 万8万9万...这样的顺子末尾处理不当

**如果听牌检测失败**:

原因可能是:
1. `TingChecker` 没有正确调用 `WinChecker.check_win()`
2. 13张转14张的逻辑有问题
3. `_get_relevant_suits()` 遗漏了某些花色

---

## 🔧 修复方案

### 方案 A: 详细日志法

在 `WinChecker.check_win()` 中添加调试日志：

```gdscript
# 在方法开头添加
print("\n【WinChecker.check_win 调试】")
print("输入手牌数: %d" % cards_to_check.size())

# 在对子循环中添加
for suit_idx in range(4):
    for num_idx in range(1, 10):
        var card_key = "%d_%d" % [suit_idx, num_idx]
        if card_key in pair_map and pair_map[card_key] >= 2:
            print("  尝试对子: %d_%d" % [suit_idx, num_idx])
            # 检查后打印结果
```

### 方案 B: 递归追踪法

在 `_can_form_melds_debug()` 中查看递归过程

### 方案 C: 单元测试法

逐个测试小的手牌组合，确认基础功能

---

## 📋 逐步修复计划

### 第1步: 收集调试输出
1. 在Godot编辑器中运行游戏
2. 查看底部输出面板的调试信息
3. 记录或复制所有输出

### 第2步: 分析输出
检查:
- [ ] 清一色测试能否通过
- [ ] 听牌测试能否通过
- [ ] 是否有错误信息

### 第3步: 定位问题
根据输出确定:
- 是对子选择问题
- 还是面子识别问题
- 还是其他

### 第4步: 修复代码
根据问题类型修改:
- `check_win()`
- `_can_form_melds()`
- 或 `TingChecker`

### 第5步: 重新测试
运行完整的单元测试验证修复

---

## ✨ 后续步骤

### 如果算法修复成功 ✅
```
→ 立即运行完整的单元测试
→ 确保所有Week 5/6测试通过
→ 继续Week 11 (GameUI开发)
```

### 如果算法修复失败 ❌
```
→ 重新审查递归逻辑
→ 查看相似的开源麻将实现
→ 考虑使用不同的算法
```

---

## 💡 关键提示

1. **保存输出**：复制调试输出到文件以便分析
2. **一次一个**：修复一个问题后再改下一个
3. **保留原始代码**：在修复前备份好
4. **增量调试**：每次修改后立即测试

---

## 🚀 今天的目标

- [ ] 运行调试脚本，获取具体输出
- [ ] 分析输出，确定问题根源
- [ ] 修复第一个问题
- [ ] 验证修复有效

**预计完成时间**: 2-3小时

---

**最后更新**: 2025-10-28
**状态**: 准备调试 🔍
