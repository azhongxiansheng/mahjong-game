class_name IBattleController extends RefCounted

# 麻将王 — E2-02：轻量共同 marker / lifecycle 基类。
# authority 状态机字段与 apply_action / run_to_end 等见 IAuthoritativeBattleController。
# NetworkedBattleController 仅应 extends 本类（不继承 authority）。
