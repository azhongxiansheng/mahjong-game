# AGENTS.md

> 本文件规定本仓库的 **Agent 工作流与开发原则**。`CLAUDE.md` 只描述项目结构与技术事实；行为约束、流程闸门、发布纪律以本文件为准。
>
> 原则提炼自 `~/lovbrowser/AGENTS.md` 与 `~/lovbrowser/CLAUDE.md` 的实战经验，并按 mahjong-game 的现状（Godot 4.5 客户端 + Go 健康检查桩，无后端、无 CI、无 TDD 框架）做了适配。

## 沟通语言
- 与用户沟通默认使用中文；用户明确指定其他语言时再切换。
- 代码注释、日志、commit message 也优先中文，除非现有文件已是英文。

## 优先级
当本文件与 `CLAUDE.md`、用户即时指令、系统默认行为冲突时：
1. **用户即时指令** —— 最高
2. **本文件 + `CLAUDE.md`** —— 高
3. 默认系统行为 —— 最低

发布、安全、版本兼容、SQL 上线类硬约束不因本文件之外的便利说法而放松。

---

## 编码四条硬纪律（CLAUDE 优先原则）

### 1. 先思考，再编码
- 实现前**显式写出假设**；不确定的事情不得假装确定。
- 需求有多种合理解释时，**必须先列出分歧与取舍**，不能静默选择其一。
- 若存在更简单、范围更小的实现路径，先说明并优先采用简单方案。
- 关键信息不足或存在真实歧义时，**暂停并向用户确认**，不要硬猜。

### 2. 简单优先
- 只写解决当前问题所需的**最小代码或最小改动**。
- 不做未被请求的功能、抽象、配置化、"顺便支持以后"。
- 不为单次需求引入一次性之外的通用层；若 200 行能压到 50 行且不损失清晰度，优先简化。
- **不为不可能发生的场景增加防御性错误处理**。

### 3. 外科手术式修改
- 只修改完成当前任务**所必需的**文件、代码、注释与格式。
- 不顺手"优化"邻近代码；保持既有风格与结构。
- 只清理**由本次改动直接造成的**废弃 import、变量、函数、注释；已有无关死代码只报告，不擅自删除。
- 每一处修改都必须能直接追溯到当前需求；不能追溯的，默认不改。

### 4. 目标驱动执行
- 把任务改写为**可验证目标**再实施，避免"做到差不多"为止。
- 多步骤任务先给出简短计划：步骤、验证方式、成功标准。
- 能用测试验证的改动，优先先写失败用例或复现步骤，再实现通过。
- 无法合理自动化测试的微小任务也必须给出最小可执行的验证方式，**不能跳过验证**。

---

## 实现前三道强制闸门

### 闸门 A：意外文件处理
开发过程中如果发现"我没有主动创建/修改"的异常文件或变更（例如 `.DS_Store`、未知临时文件、非预期 diff、Godot 的孤儿 `.uid`、根目录又冒出来的状态报告 markdown）：
1. **立刻暂停继续开发**；
2. 向用户报告发现了什么；
3. 询问处理方式（忽略 / 删除 / 纳入 / 其他）；
4. 获得明确指令后再继续。

绝不擅自删除或纳入。

### 闸门 B：计划输出
进入实现前，先给出**可执行计划**，至少包含：
- 关键假设 / 歧义
- 改动范围（哪些文件）
- 验证方式（怎么证明改对了）
- 风险点
- 成功标准

存在多种解释或更简单方案时必须在计划中先说明取舍，不能直接静默实现。

### 闸门 C：UI / 交互改动需 ASCII 草图
涉及前端 UI / 交互改动（包括 Godot 场景的布局、UI 控件、动画方向）：
- 计划中**必须提供 ASCII 草图**并等待用户确认后再实现。
- 同时涉及多种屏幕形态时（如桌面端 + 移动端、横屏 + 竖屏），分别给出对应草图。

---

## TDD 开发规范（强制）

- 默认对**功能开发 / 缺陷修复**采用 TDD（Red → Green → Refactor），除非用户明确允许跳过。
- 顺序必须是：
  1. **Red**：先写测试并运行到失败；
  2. **Green**：再写最小实现使测试通过；
  3. **Refactor**：最后重构并保持测试持续通过。
- **禁止先写业务实现再补测试**；发现偏离立即停止回到 Red。
- 文档更新、纯发布执行、纯配置搬运、无法合理自动化测试的微小改动可采用更轻量验证，但**必须先说明理由**并执行最小可验证检查。
- 每次提交/PR 必须包含：新增或更新的测试用例 + 与本次改动直接相关的测试命令与结果。

### 本仓库的 TDD 落地适配
mahjong-game 当前没有 GDScript 单元测试运行器，也没有 CI。具体落地方式：
- **GDScript 业务逻辑**（牌堆、胡牌、听牌等纯逻辑）：在 `godot/scripts/test_*.gd` 中加断言式测试场景，并通过对应 `test_*.tscn` 在 Godot 编辑器（F6）跑通。
- **UI/纹理/场景**：用 `test_mahjong_display.tscn` / `test_texture.tscn` 类似的可视化场景做"对比通过"测试。
- **Go 桩服务器**（`main.go`）：当前无测试且不应扩张其职责（详见 `CLAUDE.md`）。如需修改，先写 `_test.go`。
- **完全无法自动化的修改**（例如调 Godot 编辑器中的导入参数）：必须明确说出最小手动验证步骤（"打开 X 场景，按 F6，看到 Y"），不许跳过。

---

## 第三方接口对接顺序（强制）

对接任何第三方接口（Godot 引擎 API、外部 HTTP/WS、SDK、操作系统 API）前：

1. **先查阅并记录第三方官方资料 / 接口文档**，不得凭印象直接开发。
   - Godot 4.5 API：用 `context7` MCP 取官方文档，或在 `godot/` 内 grep 已有正确用法。
   - 训练数据可能与当前版本不一致，**怀疑印象，验证文档**。
2. 写业务实现前必须先用**最小请求测通第三方接口**，确认：认证方式、请求参数、响应结构、错误码、限流/重试要求。
3. 接口测通后，**先根据已确认的真实行为编写测试用例**，再进入业务实现。
4. 若第三方接口无法在本地或测试环境实际调用，必须先说明原因、记录替代验证方式，并经用户确认后再继续。

---

## Git 工作流

### Worktree First
- **业务代码开发默认使用 `git worktree`**，不直接在主工作区改业务代码。
- 如果当前工作区已经位于非 `main` 的任务分支，或本身就是某个任务的 worktree，**直接在当前分支/工作区继续**，不要重复新建。
- 如果在已有任务工作流之外再开新隔离分支，最终只能合并回**创建 worktree 时使用的基分支/目标分支**，不能默认合并回 `main`。
- **纯打包发布任务**（不修改仓库跟踪内容：本地打包、远端部署、产物校验、打 tag、创建 Release 等）可例外直接在主工作区执行。
- 如果任务目标虽是发布，但实际要修改业务代码、脚本、配置、文档、版本逻辑等仓库跟踪内容，仍必须先 worktree。
- worktree 提交已合并回基分支且不再需要时，应删除本地 worktree（`git worktree remove .worktrees/<task-name>`）。

mahjong-game 没有 lovbrowser 那种 `scripts/common/create-worktree.sh`，直接：
```bash
git worktree add .worktrees/<task-name> -b <branch-name> [<base-branch>]
```

### 提交与推送
- Conventional Commits：`feat:` / `fix:` / `docs:` / `refactor:` / `chore:`，必要时带 scope（如 `feat(godot/hu_rule):`）。
- **本地一旦生成提交，默认尽快 `git push`**；如需暂不推送，必须由用户明确说明。
- 每个 commit 应独立可构建；避免把不相关的 Godot 与 Go 改动混在一起。

---

## 记忆与文档固化
- 不依赖对话短期记忆；**重要上下文与决策必须固化到仓库文档**（`CLAUDE.md`、`AGENTS.md`、`docs/` 下的设计说明、Issue），而非只留在对话里。
- 关键行为变更需同步更新对应文档。
- 发现 `CLAUDE.md` 与代码事实不符时，**先更新文档，再继续工作**。

---

## 本仓库特别提醒（与上述原则联动）

> 详细的项目事实见 `CLAUDE.md`。这里只列出 agent 行为约束相关的特别项。

- **不要相信根目录的 200+ 状态/进度 markdown** —— 它们大多互相矛盾且陈旧。"先思考再编码"在这里意味着先看代码，再看 `CLAUDE.md`，最后才（谨慎地）参考根目录的历史 markdown。
- **不要扩张 `main.go` 的职责** —— 它只是 Railway 健康检查桩；新增后端逻辑不属于"外科手术式修改"，应先与用户对齐再动。
- **不要新增根目录 markdown 状态报告** —— 任何"完成总结 / 进度 / 阶段报告"都属于在已经爆炸的目录上再加噪音。需要总结的写进 commit message 或 `docs/`。
- **Godot autoload 顺序与纹理不变量是隐式契约**（详见 `CLAUDE.md`）—— 修改 `TextureExtractor`、tile 尺寸、`WHITE` 调制 / `NEAREST` 滤波之前，先看最近几个相关 commit。
- **网络相关改动不能在本仓库端到端验证** —— 客户端期望的 WebSocket 服务器不在本仓库。完成网络代码改动时必须**显式声明"未端到端验证"**，不要假装"测试通过"。
- **生成 / 替换游戏资产前先 smoke test** —— `godot/tools/asset_gen/` 是 gpt-image-2 资产管线(详见 `CLAUDE.md` 的"资产生成 (gpt-image-2)"段)。gateway 偶尔忽略 `size` 参数 + 整排 sheet 可能粘连切不开,**新接入时先用 `generate_tiles.py --smoke` 4 张样品锁风格 + 验证 b64_json 管线通**,再批量;凭证只读 `~/.zshrc` 环境变量,不入仓库。
- **资产替换或新增 `class_name` 后必须 `godot --headless --path godot --import`** —— Godot 4 的 `.godot/imported/*.ctex` 纹理缓存 + GDScript 全局类索引都不会自动跟同名 PNG / 新 `class_name` 走。缺这一步,下次启动会拍出"Parse Error: Identifier X not declared" / "Unable to open file ctex"一连串错且**界面全黑**(autoload `TextureExtractor.get_tile_texture` 全返 null)。`CLAUDE.md` 也提到这条,但坑大,在这里再钉一遍。
- **资产中间产物不入库** —— `godot/tools/asset_gen/{_raw_*,_staging*,_samples,__pycache__}/` 已 `.gitignore`(本目录内 `.gitignore`);只入库 `*.py` 脚本和最终落到 `godot/assets/` 的 PNG。生成过程产生的几十/上百 MB 原图请勿 `git add`。
