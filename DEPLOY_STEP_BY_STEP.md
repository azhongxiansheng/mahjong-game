# 🎯 麻将游戏部署 - 完整逐步指南

**您的情况**：Railway 部署失败，需要配置启动命令

**目标**：让麻将游戏在线运行

**预计时间**：10 分钟

---

## 📋 第 1 部分：准备（2 分钟）

### 第 1 步：打开浏览器

```
操作：打开您的浏览器（Chrome、Edge 等任何都可以）
验证：看到浏览器窗口打开
```

### 第 2 步：访问 Railway 仪表板

```
操作：在地址栏输入或粘贴这个链接：

https://railway.app/dashboard

然后按 Enter

验证：看到 Railway 仪表板页面
```

### 第 3 步：登录（如果需要）

```
操作：
1. 如果显示 "Login"，点击用 GitHub 登录
2. 使用您的 GitHub 账户 (azhongxiansheng) 登录

验证：看到您的项目列表
```

---

## 📋 第 2 部分：找到项目（2 分钟）

### 第 4 步：找到 mahjong-game 项目

```
操作：
在仪表板中，您应该看到左侧一个红色的卡片：

【mahjong-game】
  ❌ Failed (2 minutes ago)

这就是失败的项目

验证：找到这个项目卡片
```

### 第 5 步：点击进入项目

```
操作：
点击这个项目卡片或项目名称

验证：打开项目详情页
```

---

## 📋 第 3 部分：进入设置（1 分钟）

### 第 6 步：找到 Settings

```
操作：
在项目详情页的顶部导航栏，您会看到几个标签页：

【Details】【Build Logs】【Deploy Logs】...【Settings】

点击最右边的 "Settings" 标签

验证：进入设置页面
```

### 第 7 步：向下滚动找到启动命令

```
操作：
在 Settings 页面中，向下滚动

找到这个部分：

【Build & Deploy】
或
【Start Command】

验证：看到输入框
```

---

## 📋 第 4 部分：配置启动命令（2 分钟）

### 第 8 步：填写启动命令

```
操作：
找到标记为 "Start Command" 的输入框

点击这个输入框（使其获得焦点，看到光标）

验证：输入框有蓝色边框或光标闪烁
```

### 第 9 步：清空旧内容（如果有）

```
操作：
1. 使用 Ctrl+A 选中所有文本
2. 按 Delete 或 Backspace 删除

或者直接跳过，直接输入新内容会自动替换

验证：输入框是空的（或准备好输入）
```

### 第 10 步：输入正确的启动命令

```
操作：
在 Start Command 输入框中，输入这个精确的文本：

cd backend && go run main.go

不要改动，完全复制这个文本！

验证：
输入框显示: cd backend && go run main.go
```

---

## 📋 第 5 部分：保存设置（1 分钟）

### 第 11 步：找到保存按钮

```
操作：
在 Settings 页面中，向下或向右看

找到这样的按钮：

【Save】或【Update】或【Apply】

通常是绿色或蓝色按钮

验证：看到保存按钮
```

### 第 12 步：点击保存

```
操作：
点击 "Save" 或 "Update" 按钮

验证：
看到消息：
✓ "Settings saved" 或
✓ "Configuration updated"
```

---

## 📋 第 6 部分：重新部署（2 分钟）

### 第 13 步：返回项目主页

```
操作：
点击左上角的项目名称或返回按钮

验证：回到项目详情页
```

### 第 14 步：找到重新部署按钮

```
操作：
在项目页面中，找到这样的按钮：

【Redeploy】或【Relaunch】或【Deploy】

通常在项目卡片上或顶部工具栏中

验证：看到重新部署按钮
```

### 第 15 步：点击重新部署

```
操作：
点击 "Redeploy" 按钮

验证：
看到部署开始的信息
```

---

## 📋 第 7 部分：等待部署完成（5 分钟）

### 第 16 步：观察部署进度

```
操作：
等待部署完成

您会看到进度显示：
✓ Building...
✓ Running...

不要关闭页面！

验证：进度条或日志输出
```

### 第 17 步：检查状态

```
操作：
等待状态变为：

✅ Status: Running (绿色)

或者日志中显示：
"🚀 服务器启动在端口 :8080"

验证：看到 "Running" 状态
```

---

## ✅ 最后：验证部署成功

### 第 18 步：测试 API

```
操作：
1. 在 Railway 项目中，找到您的公网 URL
   (看起来像: https://mahjong-game-xxx.railway.app)

2. 在浏览器新标签页中，访问：
   https://your-url.railway.app/api/health

   (用实际的 URL 替换)

验证：
看到 JSON 响应：
{"status":"ok","timestamp":"..."}

这表示部署成功！ ✅
```

---

## 🎯 快速总结（如果忘记了）

```
第 1 步：打开 https://railway.app/dashboard
第 2 步：点击 mahjong-game 项目
第 3 步：点击 Settings
第 4 步：找到 Start Command
第 5 步：输入：cd backend && go run main.go
第 6 步：点击 Save
第 7 步：点击 Redeploy
第 8 步：等待 5 分钟看到 "Running"
第 9 步：测试 /api/health 端点
第 10 步：完成！✅
```

---

## 📸 如果卡住了

每一步都有预期的"验证"结果。如果您没看到预期结果，停下来告诉我：

- 您在第几步
- 您看到的是什么
- 您期望看到的是什么

我会帮您解决！

---

## 🚀 现在立即开始！

**打开浏览器，访问：**
```
https://railway.app/dashboard
```

**然后按照上面的步骤一步步操作！**

完成后告诉我结果 ✅
