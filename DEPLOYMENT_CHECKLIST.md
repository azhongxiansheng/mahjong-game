# ✅ 麻将游戏部署检查清单

## 📊 部署状态跟踪

使用此清单跟踪部署的每个步骤。打 ✅ 表示完成，❌ 表示失败。

---

## 第 1 阶段：准备工作

- [ ] 已打开浏览器
- [ ] 已访问 https://railway.app/dashboard
- [ ] 已用 GitHub 账户登录
- [ ] 能看到项目列表

---

## 第 2 阶段：进入项目

- [ ] 找到 **mahjong-game** 项目（红色/失败状态）
- [ ] 点击进入项目详情页
- [ ] 能看到项目的日志和状态

---

## 第 3 阶段：进入设置

- [ ] 在顶部导航栏找到 **Settings** 标签
- [ ] 点击进入 Settings 页面
- [ ] 页面加载完成

---

## 第 4 阶段：配置启动命令

### 查找 Start Command 字段

- [ ] 在 Settings 页面中向下滚动
- [ ] 找到标记为 **"Start Command"** 的输入框
- [ ] 输入框可见且可点击

### 输入启动命令

- [ ] 点击 Start Command 输入框
- [ ] 清空原有内容（使用 Ctrl+A + Delete）
- [ ] **精确输入**：`cd backend && go run main.go`
  - [ ] 没有多余空格
  - [ ] 没有改动任何字符
  - [ ] `&&` 符号正确
- [ ] 确认输入框中显示的是正确的命令

---

## 第 5 阶段：保存设置

- [ ] 在页面上找到 **Save** 或 **Update** 按钮
- [ ] 点击按钮
- [ ] 看到成功消息（如 "Settings saved" 或 "Configuration updated"）
- [ ] 没有看到错误信息

---

## 第 6 阶段：重新部署

### 返回项目主页

- [ ] 点击左上角返回或项目名称
- [ ] 回到项目详情页

### 触发重新部署

- [ ] 找到 **Redeploy** 或 **Relaunch** 按钮
- [ ] 点击按钮
- [ ] 看到部署开始的提示信息

---

## 第 7 阶段：监控部署过程

- [ ] 看到部署进度（"Building..." 或 "Running..."）
- [ ] 日志开始输出
- [ ] **不要关闭浏览器页面！**

### 部署完成标志

等待以下状态之一出现：

- [ ] **Status: Running** (绿色指示灯)
- [ ] 日志显示：**"🚀 服务器启动在端口 :8080"**
- [ ] 没有看到红色错误信息

---

## 第 8 阶段：验证部署成功

### 获取应用 URL

- [ ] 在 Railway 项目中找到您的公网 URL
  - 通常看起来像：`https://mahjong-game-xxx.railway.app`
- [ ] 复制这个 URL

### 测试 API 端点

- [ ] 打开浏览器新标签页
- [ ] 在地址栏访问：`https://your-url.railway.app/api/health`
  - （用实际的 URL 替换 `your-url`）
- [ ] 看到 JSON 响应：
  ```json
  {"status":"ok","timestamp":"2025-..."}
  ```

---

## 🎉 部署成功标志

所有以下条件都满足时，部署成功：

```
✅ Status: Running (绿色)
✅ 日志无错误
✅ /api/health 端点返回 OK
✅ 能访问公网 URL
✅ JSON 响应正确
```

---

## ❌ 常见问题排查

如果部署失败，检查以下几点：

### 问题 1：Start Command 显示错误

```
症状：Settings 中的 Start Command 显示错误或无法保存

解决：
1. 清空输入框
2. 确保没有多余空格
3. 再次输入：cd backend && go run main.go
4. 点击 Save
```

### 问题 2：部署仍然失败

```
症状：点击 Redeploy 后，Status 仍显示 Failed

解决：
1. 打开 Build Logs 标签
2. 查看具体的错误信息
3. 记下错误内容
4. 告诉我错误信息的前几行
```

### 问题 3：/api/health 无法访问

```
症状：访问 URL 时，浏览器显示错误

解决：
1. 确认 Status 确实显示 Running
2. 等待 1-2 分钟（应用可能还在启动）
3. 刷新浏览器页面（F5）
4. 再次尝试访问 /api/health
```

---

## 📞 获取帮助

如果您在任何步骤卡住，请提供以下信息：

1. **您在第几步卡住**
   - 例如："第 4 步 - 输入启动命令"

2. **您看到的是什么**
   - 描述或截图

3. **期望看到什么**
   - 根据本清单中的说明

4. **错误信息（如果有）**
   - 复制完整的错误文本

---

## 🎯 下一步（部署成功后）

部署成功后，您可以：

1. **分享 URL** - 其他人可以访问您的麻将游戏
2. **配置 WeChat Mini-Program** - 如果需要微信集成
3. **监控性能** - 使用 Railway 的监控工具
4. **设置自定义域名** - (高级功能)

---

**祝您部署顺利！** 🚀🎲
