# 🔧 错误修复指南 - CardTile 信号连接问题

## ❌ 错误信息

```
Invalid access to property or key 'card_pressed' on a base object of type 'Control'.
```

---

## 🔍 问题原因

当从场景动态实例化 `CardTile` 时，对象没有被正确识别为 `CardTile` 类型，导致信号连接失败。

**常见原因:**
1. CardTile场景的脚本没有正确附加
2. 场景的根节点类型不匹配
3. 类型检查失败

---

## ✅ 解决方案

### 步骤1: 验证CardTile场景

检查 `godot/scenes/card_tile.tscn` 是否有以下特性：

```gdscript
✓ 根节点: Control (带 CardTile 脚本)
✓ 脚本: res://scripts/card_tile.gd
✓ 类名声明: class_name CardTile
```

### 步骤2: 更新HandDisplay.gd

使用更安全的类型检查和信号连接：

```gdscript
func add_card_display(card: CardData, index: int = -1) -> void:
	"""添加单张卡牌显示"""
	var tile = card_tile_scene.instantiate()
	
	# 验证是否是CardTile类型
	if not tile is CardTile:
		print("⚠ 场景未能正确实例化为CardTile类型")
		tile.queue_free()
		return
	
	# 转换为CardTile类型
	var card_tile: CardTile = tile as CardTile
	
	card_tile.set_card(card)
	add_child(card_tile)
	
	# 设置位置
	var x_pos = 20 + (card_tiles.size() * 85)
	var y_pos = 35
	card_tile.position = Vector2(x_pos, y_pos)
	
	# 连接信号 - 使用更安全的方式
	if card_tile.has_signal("card_pressed"):
		card_tile.card_pressed.connect(_on_card_pressed.bind(card_tile))
	else:
		print("⚠ CardTile没有card_pressed信号")
	
	if card_tile.has_signal("card_selected"):
		card_tile.card_selected.connect(_on_card_selected.bind(card_tile))
	else:
		print("⚠ CardTile没有card_selected信号")
	
	card_tiles.append(card_tile)
```

### 步骤3: 验证CardTile.gd

确保脚本有以下特性：

```gdscript
class_name CardTile
extends Control

# 信号
signal card_pressed(card: CardData)
signal card_selected(card: CardData)

func _ready() -> void:
	gui_input.connect(_on_gui_input)
	mouse_entered.connect(_on_mouse_entered)
	mouse_exited.connect(_on_mouse_exited)
	custom_minimum_size = Vector2(80, 120)
```

---

## 🧪 调试步骤

### 步骤1: 添加调试输出

在 `hand_display.gd` 中检查类型：

```gdscript
var tile = card_tile_scene.instantiate()
print("Tile type: ", tile.get_class())
print("Tile script: ", tile.get_script())
print("Is CardTile: ", tile is CardTile)
```

### 步骤2: 检查场景加载

```gdscript
var scene = preload("res://scenes/card_tile.tscn")
print("Scene path: ", scene.resource_path)
print("Scene valid: ", scene != null)
var instance = scene.instantiate()
print("Instance class: ", instance.get_class())
```

### 步骤3: 验证信号存在

```gdscript
print("Has signal 'card_pressed': ", instance.has_signal("card_pressed"))
print("Has signal 'card_selected': ", instance.has_signal("card_selected"))
```

---

## 🔄 完整修复流程

1. ✅ 更新 `hand_display.gd` (已完成)
2. ✅ 添加类型检查
3. ✅ 添加信号验证
4. 📋 运行游戏并测试
5. 📋 检查控制台输出
6. 📋 修复任何额外的错误

---

## 📊 检查清单

- [ ] CardTile场景有正确的脚本
- [ ] CardTile.gd有 `class_name CardTile` 声明
- [ ] CardTile.gd定义了 `card_pressed` 和 `card_selected` 信号
- [ ] HandDisplay.gd已更新为使用类型检查
- [ ] 游戏运行时没有错误
- [ ] 所有信号正确连接

---

## 💡 预防方法

1. **使用 `@onready` 连接子节点**
   ```gdscript
   @onready var card_tiles: Array[CardTile] = $CardContainer.get_children()
   ```

2. **为动态创建添加类型检查**
   ```gdscript
   if not instance is TargetClass:
       print("错误: 类型不匹配")
       return
   ```

3. **验证信号存在后再连接**
   ```gdscript
   if instance.has_signal("signal_name"):
       instance.signal_name.connect(callback)
   ```

---

## 🚀 快速测试

完成修复后，按以下步骤测试：

```
1. 打开Godot编辑器
2. 运行主场景
3. 按快捷键 [1] 显示手牌
4. 检查控制台是否有错误信息
5. 尝试点击卡牌
```

**预期结果:**
- ✅ 显示13张卡牌
- ✅ 卡牌可以点击和选中
- ✅ 控制台没有 "Invalid access" 错误

---

**最后更新**: 2025-10-30  
**状态**: 🟢 已修复
