# Playtest 会话 — \<日期 YYYY-MM-DD\> alpha \<N\> / beta batch \<N\>

> **使用方法**：复制本文件改名（如 `2026-05-15-alpha-1.md`），按段填写。**会话当时记录**，不要回填。
>
> 删除本提示行后再 commit。

## 元信息

- **测试者**：\<姓名 / 别名\>
- **会话时长**：\<开始 - 结束\>
- **会话期间 main HEAD**：`<git rev-parse HEAD>`（pin 住版本，方便后续追溯）
- **测试环境**：Godot 版本 / OS / 硬件备注（若与默认偏离）
- **跑了几个 Run**：\<n\>

## 会话概要

\<2-3 句话总结：玩了什么、卡在哪、整体感受是什么\>

---

## 每个 Run 的快速记录

复制下面这段 N 次，每个 Run 一段：

### Run #\<n\>

| 字段 | 值 |
|---|---|
| 起始包 | 火力 / 速胡 / 控场 |
| seed | `<seed>` 或"未记录" |
| 通关章节 | 章 1 / 章 2 / 章 3 / 失败于章 \<X\> 节点 \<Y\> |
| 最终 HP | \<n\>/\<max_hp\> |
| 最终金币 | \<n\> |
| 关键卡组组合（前 5 张） | `<id1>` / `<id2>` / ... |
| 节点排名分布 | rank1×\<n\> / rank2×\<n\> / rank3×\<n\> / rank4×\<n\> |
| 主观难度（1-5，1 = 太简单 5 = 太难） | \<n\> |
| 主观乐趣（1-5） | \<n\> |

**爽点**：\<具体场景：哪一手胡牌 / 哪个技能 combo 让你"哇"\>

**不爽点**：\<具体场景：哪一节点感觉无意义 / 哪个技能太弱或太强 / 哪个 UI 卡顿\>

**期望调整**（不写代码、只写主观期望）：\<例："1 万·闪电太弱了，想要 +2 番"\>

---

## 横向观察

跨 Run 的模式（只有跑了 ≥ 3 Run 时填）：

- **某起始包是否过强 / 过弱**：\<观察\>
- **某技能是否每次必抓 / 必跳过**：\<观察 + 推测原因\>
- **抽卡稀有度命中是否符合直觉（普 60% / 精 28% / 史 10% / 神 2%）**：\<观察\>
- **节点路径选择倾向**（普通 / 精英 / 营地 / 商店 / 事件 / Boss）：\<观察\>

## 提取的"假设"

会话结束时，把上面的观察提炼成 1-3 条具体可验证的假设（按 plan-7 D6 三步法）：

1. **假设 X**：\<例："起始包 火力 通关率 > 80%（远高于设计目标 30-50%）"\>
   - **下一步**：跑 `simulate_runs --runs=1000 --starter=fire` 验证；若确认调 `BalanceConstants.lookup(&"thunder_5w_han_bonus")` 从 1 → 0
2. **假设 Y**：\<...\>
3. **假设 Z**：\<...\>

## 不爽 / 漏洞汇总

- **明显 bug**（与设计意图冲突）：\<列表 + 复现步骤\>
- **数值平衡不爽**（不是 bug 但体感差）：\<列表\>
- **UX 摩擦**（操作 / 提示 / 反馈不清）：\<列表\>

## 后续动作

完成本表 commit 后：
- [ ] 把 \<假设 X / Y / Z\> 加进下一次 simulation 验证清单
- [ ] 明显 bug 起 issue 或直接修
- [ ] 数值不爽走 plan-7 D6 三步法，下一次 alpha 复跑前不动

---

\<可选：截图 / 录屏链接\>
