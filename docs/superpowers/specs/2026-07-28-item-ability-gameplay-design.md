# 欢乐场道具能力玩法设计（v1 草案）

> 日期：2026-07-28
> 状态：玩法设计稿，尚未授权修改生产效果
> 适用模式：嘴强欢乐场；标准场继续在构造期禁用全部道具
> 事实基线：`ItemCatalog` 当前登记 10 个战斗消耗品、12 个遗物，其中 19 个可发放

## 1. 设计目标

道具应放大欢乐场的判断、逆转和嘴仗反馈，但不能取代日麻的基本决策：

1. **先有合法和牌，再结算道具加成。** 道具提供的番、Dora 不得凭空创造役。
2. **效果可预测。** 玩家在使用前能从一句规则文本判断触发时机、对象、持续时间与消耗条件。
3. **信息只影响决策，不篡改客户端权威。** 窥牌结果仅向受益席投影，牌墙与结算仍由 Worker 决定。
4. **多实例可解释。** 同 ID 实例仍保持独立身份；消耗品逐件使用，遗物按本文规则线性或分层叠加。
5. **避免同质化。** 防御、信息、节奏、Dora、条件增番和逆转各有不同决策窗口。
6. **不为首版强行启用高风险能力。** 涉及换座、整手重洗和抽卡保底的 3 件道具继续不可发放。

## 2. 全局规则

### 2.1 道具类型

| 类型 | 获取后状态 | 使用方式 | 跨局 | 多实例 |
|---|---|---|---|---|
| 即时消耗品 | `held` | 在自己的合法决策窗口主动使用，成功后立即生效并消耗 | 未使用实例保留到整场结束 | 每次命令只指定并消耗 1 个实例 |
| 武装消耗品 | `held` | 主动使用后变为 `armed`，命中条件时生效并消耗 | 未触发时跨手牌保留武装 | 同 ID 同席同时最多武装 1 个，其余保持 `held` |
| 遗物 | 获得后立即生效 | 无主动按钮 | 保留到整场结束 | 每个实例独立注册；叠加方式由单件规则明确 |

主动使用只允许发生在使用者拥有行动权、牌局未进入 CLAIM/结算/奖励屏障时。失败的使用不消耗实例。

### 2.2 结算顺序

同一事件按以下玩法顺序处理：

1. 校验日麻原始行动与原始役；
2. 处理“取消荣和”类效果；
3. 处理条件增番、额外 Dora；
4. 处理番数倍率与上限；
5. 计算本次和牌点数；
6. 处理基于“本次实际得点”的返还或转移；
7. 发布 `ITEM_APPLIED` / `ITEM_CONSUMED`，客户端只投影权威结果。

同一弃牌出现多家荣和时，取消类道具以完整 CLAIM 结果为边界，不依赖客户端动画先后。本文若写“本次弃牌产生的荣和”，指该弃牌的整个多家荣和集合。

### 2.3 数值与取整

- 点数返还/转移统一向下取整到 100 点。
- 额外 Dora、额外赤 Dora 只增加计分番，不视为独立役。
- 负番防御只减少计分番，不移除原始役；最终计分番最低为 1。
- 不同名称的效果可以叠加；同 ID 武装消耗品不能同时武装多件。
- 遗物无通用叠加上限；需要非线性叠加的道具在自身规则内明确。

## 3. 消耗品正式玩法

### 3.1 首发 8 件

| ID / 名称 | 稀有度 | 类型 | 正式规则文本 | 叠加与边界 | 落地级别 |
|---|---|---|---|---|---|
| `iron_shield_v1` 铁盾 | 稀有 | 武装 | 武装后，你下一次打出的牌若产生荣和，取消该弃牌产生的全部荣和，然后消耗。 | 对自摸无效；一次保护整个多家荣和集合。 | L2 |
| `wall_peek_v1` 千里眼 | 普通 | 即时 | 查看当前牌墙接下来 3 张牌的顺序。信息仅你可见，直到这些牌离开牌墙或本局结束。 | 多次使用会刷新并扩大当前可见集合，不改变牌序。 | L1 |
| `double_payout_v1` 倍率券 | 史诗 | 武装 | 你下一次合法胡牌时，结算前将总番数翻倍；由此额外获得的番数最多为 3，然后消耗。 | 在额外番、Dora 加入后计算；役满不再放大。 | L2 |
| `dora_charm_v1` 宝牌护符 | 史诗 | 武装 | 你下一次合法胡牌时额外获得 2 Dora，然后消耗。 | 不创造役；荣和、自摸均可触发。 | L1 |
| `wall_collapse_v1` 牌墙崩塌 | 稀有 | 即时 | 从当前活牌墙顶部移除 6 张牌，使本局更快进入海底。 | 不得令活牌墙少于 14 张；条件不足时拒绝且不消耗。 | L1 |
| `dora_flip_v1` 翻宝牌 | 稀有 | 即时 | 立即为所有玩家翻开 1 张额外宝牌指示牌。 | 已达到本局可翻上限时拒绝且不消耗；这是公开的对称效果。 | L2 |
| `furiten_bomb_v1` 振听炸弹 | 史诗 | 武装 | 武装后，下一名对手声明的荣和被取消，然后消耗。 | 不要求该荣和来自你的舍牌；对自摸无效。这是欢乐场的全桌干扰牌。 | L0 |
| `point_shield_v1` 点棒护盾 | 史诗 | 武装 | 你下一次放铳正常结算后，从和牌者本次因荣和获得的点数中返还 30% 给你，然后消耗。 | 只按本次荣和净得点计算，不按和牌者总分；不返还立直棒，不影响其他付款席。 | L2 |

`落地级别` 含义：L0 为现有机制基本吻合；L1 为参数、条件或文案级调整；L2 需要扩充事件上下文或权威结算路径。它不是优先级。

### 3.2 消耗品的决策定位

- **铁盾**是可预期的个人防守牌，保护一次自己的危险弃牌。
- **振听炸弹**是全桌干扰牌，能救别人、破坏领先者，也可能改变同盟关系；其史诗稀有度用于控制出现频率。
- **点棒护盾**不否定和牌，只降低一次放铳代价，与铁盾形成“取消结果 / 缓和结果”的区别。
- **千里眼**提供计划能力；**牌墙崩塌**改变剩余时间；两者共同形成信息与节奏组合，但不直接给分。
- **翻宝牌**是公开风险投资；使用者可基于自己手牌判断收益，但其他三家同样可能受益。
- **宝牌护符**提供稳定爆发；**倍率券**奖励原本已经较高的牌型，但额外番上限避免普通和牌直接跃迁役满。

## 4. 遗物正式玩法

### 4.1 首发 11 件

| ID / 名称 | 稀有度 | 正式规则文本 | 多实例规则 | 落地级别 |
|---|---|---|---|---|
| `relic_lucky_cat_v1` 招财猫 | 稀有 | 你每次合法胡牌额外获得 1 Dora。 | 每件各 +1，线性叠加。 | L0 |
| `relic_iron_will_v1` 铁壁意志 | 稀有 | 你放铳时，和牌者的计分番数 -1。 | 每件各 -1，但最终计分番最低为 1。 | L1 |
| `relic_soul_mirror_v1` 魂镜 | 史诗 | 任一对手胡牌结算后，从该对手本次实际获得的点数中转移 5% 给你。 | 每件按原始本次得点各计算一次，逐件转移并向下取整到 100 点。 | L2 |
| `relic_wall_eye_v1` 墙眼 | 传奇 | 你每次摸牌后，查看牌墙接下来的牌。 | 1/2/3+ 件分别查看 1/2/3 张，最多 3 张；只向你显示。 | L2 |
| `relic_red_string_v1` 红线 | 稀有 | 你以门清手牌胡牌时额外获得 1 赤 Dora。 | 每件各 +1；副露手牌不触发。 | L1 |
| `relic_dragon_seal_v1` 龙印 | 稀有 | 你的胡牌包含白、发或中的刻子/杠子役时，额外 +1 番。 | 每件各 +1；同一手牌不因多种三元牌重复触发单件。 | L2 |
| `relic_wind_charm_v1` 风铃 | 稀有 | 你的胡牌包含自风或场风役时，额外 +1 番。 | 每件各 +1；连风牌仍只触发单件 1 次。 | L2 |
| `relic_speed_demon_v1` 速攻鬼 | 史诗 | 你在自己完成第 6 次舍牌前胡牌，额外 +1 番。 | 每件各 +1；使用个人舍牌数，不使用全局动作数。 | L2 |
| `relic_patience_stone_v1` 忍石 | 稀有 | 荒牌流局时，若你听牌，额外获得 1000 点。 | 每件各 +1000；途中流局不触发。 | L2 |
| `relic_han_crystal_v1` 番水晶 | 史诗 | 你立直后胡牌时额外 +1 番。 | 每件各 +1；双立直也只触发单件 1 次。 | L0 |
| `relic_comeback_crown_v1` 逆转王冠 | 史诗 | 胡牌声明时，若你严格为全场最低分，额外 +2 番；若并列最低，额外 +1 番。 | 每件独立叠加；比较的是本次胡牌结算前分数。 | L1 |

### 4.2 遗物组合预期

| 构筑方向 | 代表组合 | 玩法收益 | 主要制衡 |
|---|---|---|---|
| 早攻 | 速攻鬼 + 招财猫 | 小牌快速抬升打点 | 必须在个人第 6 次舍牌前完成 |
| 立直爆发 | 番水晶 + 红线 + 宝牌护符 | 奖励门清与立直路线 | 需要合法役且承担立直风险 |
| 字牌构筑 | 龙印 + 风铃 | 让役牌手拥有明确成长线 | 受起手牌和鸣牌公开信息制约 |
| 防守拖局 | 铁壁意志 + 忍石 | 降低放铳损失、奖励听牌流局 | 不直接帮助成牌 |
| 信息控制 | 墙眼 + 千里眼 | 规划摸牌和危险度 | 不改变牌序，也不直接给番 |
| 逆转经济 | 逆转王冠 + 魂镜 | 落后时提高追分能力 | 必须满足分数或他家胡牌条件 |

## 5. 暂不进入首发的 3 件

| ID / 当前名称 | 处理 | 原因 | 若未来重做的方向 |
|---|---|---|---|
| `seat_swap_v1` 换座 | 保持不可发放、不可使用 | 换座会同时影响玩家身份、庄家、场风/自风、网络 projection、回放与 UI 朝向，不是单一 Hook 能安全完成的效果。 | 另立“整场开始前换位”规则，不允许牌局中途使用。 |
| `tsubame_v1` 燕返 | 保持不可发放、不可使用；正式命名前避免使用该作品化黑话 | 整手重洗需要定义退回位置、补牌来源、牌实例身份、岭上/王牌边界和公开信息，且强度高度依赖使用时机。 | 改为原创名“回炉签”：仅配牌后、首个动作前，将最多 3 张选中手牌置于活牌墙底并补 3 张；需要独立交互与权威协议设计。 |
| `relic_pity_breaker_v1` 天命打破 | 保持不可发放、不可使用 | 当前“抽卡保底”依赖已退出生产的 Run/gacha 语义，与欢乐场确定性四道具分配冲突。 | 若未来需要奖励保底，应作为 RewardWindow 规则版本能力设计，不伪装成战斗遗物。 |

## 6. Affinity 建议

首版继续使用现有五类 affinity 作为垃圾话匹配标签，不让 affinity 放大道具效果。建议只调整两件的归属，使语义和数量更均衡：

| 道具 | 当前 | 建议 | 理由 |
|---|---|---|---|
| 点棒护盾 | `CALM` | `CUNNING` | “从赢家处取回一部分”更像算计，而非纯减伤。 |
| 逆转王冠 | `DOMINATION` | `MYSTIC` | 逆势触发属于命运/反转，不应继续挤占进攻类池。 |

调整后，可发放道具的主 affinity 数量为：`CALM 3 / CUNNING 4 / DOMINATION 4 / PASSION 4 / MYSTIC 4`。19 件无法五等分，让防守型 CALM 少 1 件可以减少欢乐场整体拖延。

## 7. 首轮平衡观测

实现后不先凭感觉连续改数值，而以固定 seed 的欢乐场回归记录以下指标：

| 指标 | 首轮观察目标 |
|---|---|
| 道具实际触发率 | 消耗品 35%–70%；低于 20% 说明触发条件或 UI 时机不清楚 |
| 单件平均点数影响 | 普通 < 稀有 < 史诗；信息类单独看胜率变化，不硬折算点数 |
| 取消荣和占比 | 不超过全部合法荣和的 15% |
| 平均每场未使用消耗品 | 不超过获得消耗品的 40% |
| 遗物重复实例占比 | 记录 1/2/3+ 件时的边际收益，重点观察墙眼、铁壁意志、魂镜 |
| 逆转效果 | 末位席获得逆转王冠后的最终名次改善，但不能让单件稳定抹平 12000 点以上差距 |
| 信息泄漏 | 非受益席 projection 中不得出现窥见牌的实体或顺序 |

## 8. 实现拆分建议

本稿确认后，按风险拆成三批，而不是一次改完 19 个 Hook：

1. **规则对齐批（L0/L1）**：修正文案与参数，完成宝牌护符、千里眼、铁壁意志、红线、逆转王冠等条件测试。
2. **计分上下文批（L2-score）**：为倍率上限、役种条件、个人舍牌数、本次实际得点建立只读上下文与统一结算 API，再迁移倍率券、点棒护盾、魂镜、龙印、风铃、速攻鬼、忍石。
3. **权威流程批（L2-flow）**：以完整 CLAIM 聚合铁盾的多家荣和语义，实现公开翻宝牌和墙眼多层预知。

每批都应先写真实 GUT 失败用例，再做最小实现；协议 DTO、`ItemInstance` 生命周期和 `ITEM_*` 事件包络默认不变。若某项必须改变公共事件 schema，应独立立项并升级全量验证。

## 9. 本稿默认决策

若无进一步产品修改，后续实现以以下结论为准：

- 首发只启用 19 件当前可发放道具；3 件排除项不借机实现。
- 道具不创造役，隐藏信息按席投影，点数转移以本次结算为基数。
- 同 ID 武装消耗品同时最多 1 件；遗物实例继续独立叠加。
- “翻宝牌”改成真正公开翻指示牌，不再保留“下一次摸牌凭空 +1 Dora”的占位语义。
- 铁盾保护自己一次完整放铳事件；振听炸弹保留为可干扰全桌的史诗效果。
- 先保守设定数值，再通过固定 seed 的整场数据调整，不在首轮加入套装、商店或 Run 系统。
