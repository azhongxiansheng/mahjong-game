# 🎴 卡牌样式升级 - 美化麻将牌显示

## 🎨 改进内容

之前卡牌显示太抽象，现在已升级为**真实麻将牌样式**！

### 改进对比

| 特性 | 之前 | 现在 |
|------|------|------|
| 显示方式 | 纯文字（万1、万2） | 真实牌面设计 |
| 背景 | 无 | 象牙色卡牌 |
| 边框 | 无 | 深色边框 |
| 布局 | 一行文字 | 三层设计（上/中/下） |
| 选中效果 | 黄色文字 | 高亮黄色背景+金色边框 |
| 圆角 | 无 | 有 |

---

## ✨ 新卡牌设计特性

### 1️⃣ 卡牌背景样式
```
颜色：象牙色 (RGB: 217, 204, 179)
特点：
- 温暖的米黄色，像真实麻将牌
- 深色边框 (RGB: 77, 51, 26)
- 圆角设计，看起来更柔和
- 尺寸：75×145 像素
```

### 2️⃣ 卡牌内容结构
```
┌─────────────────┐
│      万（上）   │  <- 花色标签（小字）
│                 │
│      8（中）    │  <- 数字标签（大字32像素）
│                 │
│      万（下）   │  <- 花色标签（小字）
└─────────────────┘
```

### 3️⃣ 选中效果
```
未选中状态：
- 背景：象牙色
- 边框：深棕色（2像素）

选中状态：
- 背景：高亮黄色 (RGB: 255, 242, 128)
- 边框：金色（3像素）
- 宽度：更厚，让选中效果更明显
```

### 4️⃣ 卡牌布局
```
排列方式：水平排成一行
间距：85像素 （3张卡牌间距约250像素）
垂直位置：屏幕中间偏下（Y=370）
卡牌数量：13张（标准一手牌）
```

---

## 📊 美化前后效果说明

### 之前的样子
```
[万1] [万2] [万3] [筒4] [筒5] [筒6] [条7] [条8]
 ↑
 只有白色文字，没有卡牌的感觉
```

### 现在的样子
```
┌──┐ ┌──┐ ┌──┐ ┌──┐ ┌──┐ ┌──┐ ┌──┐ ┌──┐ ┌──┐ ┌──┐ ┌──┐ ┌──┐ ┌──┐ ┌──┐
│万│ │万│ │万│ │筒│ │筒│ │筒│ │条│ │条│ │条│ │字│ │万│ │筒│ │条│
│1│ │2│ │3│ │4│ │5│ │6│ │7│ │8│ │9│ │1│ │5│ │2│ │4│
│万│ │万│ │万│ │筒│ │筒│ │筒│ │条│ │条│ │条│ │字│ │万│ │筒│ │条│
└──┘ └──┘ └──┘ └──┘ └──┘ └──┘ └──┘ └──┘ └──┘ └──┘ └──┘ └──┘ └──┘
  ↑   ↑    ↑   ↑    ↑   ↑   ↑    ↑    ↑   ↑   ↑    ↑   ↑
  有边框、背景、更立体
```

### 选中效果

**未选中**：象牙色卡牌
```
┌──┐
│万│
│5│
│万│
└──┘
```

**选中**：高亮黄色卡牌+金色边框
```
╔══╗
║万║  <- 更粗的金色边框
║5║     背景变成高亮黄色
║万║
╚══╝
```

---

## 🎯 完整卡牌尺寸参考

### 卡牌容器
```gdscript
宽度：75像素
高度：145像素
背景色：Color(0.85, 0.8, 0.7, 1.0)  # 象牙色
边框宽度：2像素
边框色：Color(0.3, 0.2, 0.1, 1.0)   # 深棕色
圆角半径：3像素
```

### 内部内容（VBox容器）
```
上部分（花色标签）：
  - 字体大小：12像素
  - 颜色：深灰色
  - 高度：25像素

中间（数字标签）：
  - 字体大小：32像素
  - 颜色：深黑色
  - 高度：50像素

下部分（花色标签）：
  - 字体大小：12像素
  - 颜色：深灰色
  - 高度：25像素
```

---

## 🎮 交互体验

### 鼠标操作
```
1. 鼠标移动到卡牌：光标变为手指（可点击）
2. 点击卡牌：
   - 卡牌变为高亮黄色
   - 边框变为3像素金色
   - 底部输出显示选中消息
   
3. 再次点击其他卡牌：
   - 前一张卡牌恢复象牙色
   - 新卡牌变为高亮黄色
```

### 按钮操作
```
出牌：删除选中的卡牌
胡：显示胡牌提示
不要：清除选择
取消：恢复前一张卡牌的象牙色
```

---

## 🔧 配置调整

### 如果想改变卡牌颜色

**象牙色背景**（修改这一行）：
```gdscript
card_style.bg_color = Color(0.85, 0.8, 0.7, 1.0)
```

可尝试的颜色：
- 白色：`Color(1.0, 1.0, 1.0, 1.0)`
- 浅绿色：`Color(0.8, 0.9, 0.8, 1.0)`
- 浅蓝色：`Color(0.8, 0.9, 1.0, 1.0)`
- 灰色：`Color(0.85, 0.85, 0.85, 1.0)`

### 如果想改变卡牌大小

**卡牌尺寸**（修改这一行）：
```gdscript
card_container.custom_minimum_size = Vector2(75, 145)
```

改成：
```
